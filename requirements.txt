# DeepSceneLoc - Requirements
# Python 3.10+
#
# ── IMPORTANT: PyTorch + CUDA ──────────────────────────────────────────────────
# Do NOT install torch from this file — the plain PyPI index ships CPU-only builds.
# Install the CUDA 12.6 wheel first (one-time, matches RTX 30/40-series drivers):
#
#   pip install torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/cu126
#
# For CPU-only (no GPU):
#   pip install torch torchvision torchaudio
# ──────────────────────────────────────────────────────────────────────────────

# Core Deep Learning (installed separately — see CUDA note above)
# torch>=2.6.0       # installed via --index-url https://download.pytorch.org/whl/cu126
# torchvision>=0.21.0
# torchaudio>=2.6.0

# Vision Transformer support
timm>=1.0.9

# Data Processing
numpy>=2.0.0
pandas>=2.2.0
Pillow>=11.0.0
opencv-python>=4.10.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.24.0

# AI Integration (Semester 2 — exact location detection)
google-generativeai>=0.8.0

# Metrics and Evaluation
scikit-learn>=1.5.0

# Progress Tracking
tqdm>=4.67.0

# Interactive Tools
gradio>=4.44.0
jupyter>=1.1.0
notebook>=7.2.0

# Web Backend (Flask API for React frontend)
flask>=3.0.0
flask-cors>=4.0.0

# Utilities
pyyaml>=6.0.2
python-dotenv>=1.0.1

# Dataset download helpers
gdown>=5.2.0
kaggle>=1.6.17
